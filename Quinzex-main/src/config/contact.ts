/**
 * Quinzex Intelligence - Contact Form Configuration
 * 
 * We use Web3Forms for client-side form submissions with zero server backend.
 * You can get a free access key in 30 seconds at: https://web3forms.com
 */
export const contactConfig = {
  // Replace this with your actual Web3Forms Access Key
  web3FormsAccessKey: "", 

  // When true (or when no key is set), form submissions are simulated locally
  // with a premium toast notification and gorgeous success states.
  enableLocalFallback: true,

  // Fallback email shown to users if submission fails
  fallbackEmail: "hello@quinzex.com",
};
